// api/bot.js

module.exports = async (req, res) => {
    // Реагируем только на POST-запросы от Telegram
    if (req.method !== 'POST') {
        return res.status(200).send('Бот Boss Store запущен и работает!');
    }

    try {
        const botToken = process.env.TELEGRAM_BOT_TOKEN;
        if (!botToken) {
            return res.status(500).send('Ошибка: TELEGRAM_BOT_TOKEN не настроен!');
        }

        const update = req.body;

        // Проверяем, что пришло текстовое сообщение
        if (update && update.message) {
            const chatId = update.message.chat.id;
            const text = update.message.text;
            const firstName = update.message.from.first_name || 'друг';

            // Если пользователь нажал /start
            if (text === '/start') {
                const welcomeText = Привет, ${firstName}! 👋\n\n Добро пожаловать в BOSS STORE [фриланс услуги] — Ваш профессиональный проводник в мире качественных цифровых решений. Мы объединили лучшие компетенции в IT, дизайне, копирайтинге и продвижении в одном удобном интерфейсе для реализации Ваших идей.

➖➖➖

Порядок оформления Вашего заказа:

1️⃣ Выбор направления: Откройте наше Web-приложение через кнопку ниже и ознакомьтесь с актуальным каталогом. Мы подготовили подробные описания и примеры для каждой позиции, чтобы Вам было проще сделать выбор.
2️⃣ Техническое задание: При добавлении выбранной услуги в корзину, пожалуйста, максимально подробно опишите Ваши требования и пожелания в поле для комментариев. Чем детальнее будет составлено Ваше ТЗ, тем точнее специалист сможет реализовать Ваш запрос.
3️⃣ Оплата заказа: На текущем этапе наш сервис поддерживает оплату исключительно через безопасную платежную систему Crypto Bot (@send). Это гарантирует моментальное подтверждение транзакции и высокую безопасность Ваших средств.

➖➖➖

Процесс выполнения и сроки:

⏱ Время работы: Сразу после подтверждения оплаты Ваш заказ автоматически поступает в работу к профильному специалисту. В течение времени вы получите уведомление с ориентировочным временем полной готовности заказа.
🛡 Гарантия качества: Мы контролируем каждый этап выполнения, чтобы итоговый результат соответствовал высоким стандартам качества BOSS STORE.

➖➖➖

Получение готового результата:

📥 Выдача работы: Вам не потребуется использовать сторонние ресурсы или почту. Все готовые материалы (файлы, макеты, тексты, программный код или ссылки на доступы) будут отправлены специалистом непосредственно вам в лс телеграм. Вся история ваших заказов и готовые материалы всегда будут у вас под рукой в диалоге с администратором.

➖➖➖

Для ознакомления с полным перечнем услуг и начала работы, пожалуйста, нажмите на кнопку «💼 Открыть BOSS STORE» ниже.

Благодарим Вас за доверие. Мы сделаем всё возможное, чтобы Вы остались довольны результатом нашего сотрудничества! 👇;

                // Создаем клавиатуру с кнопкой WebApp и ссылкой на отзывы
                const keyboard = {
                    inline_keyboard: [
                        [
                            {
                                text: "🎨 Открыть Boss Store",
                                web_app: {
                                    url: "https://magazin-kachestvo-site-vapp.vercel.app/" // Твоя ссылка на сайт
                                }
                            }
                        ],
                        [
                            {
                                text: "💬 Читать отзывы",
                                url: "https://t.me/rewiew_nm" // Ссылка на твой канал с отзывами
                            }
                        ]
                    ]
                };

                // Отправляем приветственное сообщение
                await fetch(https://api.telegram.org/bot${botToken}/sendMessage, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({
                        chat_id: chatId,
                        text: welcomeText,
                        parse_mode: 'HTML',
                        reply_markup: keyboard
                    })
                });
            }
        }

        return res.status(200).send('OK');

    } catch (error) {
        console.error('Ошибка бота:', error);
        return res.status(500).send('Internal Error');
    }
};
